using Bifrost.MSpec.Events;
using Chirp.Events.Streams;
using Machine.Specifications;

namespace Chirp.Domain.Specs.Streams.for_Stream
{
    //public class when_publishing_a_message : given.a_stream
    //{
    //    const string    message = "Its a message";

    //    Because of = () => stream.Publish(message);

    //    It should_have_a_published_event_with_message_in_it = () => stream.ShouldHaveEvent<MessagePublished>().AtBeginning().WithValues(e => e.Message == message);
    //}
}
